services:
  # Duplicate for as many smart plug + Chromecast Audios as you have
  example:
    image: edbrxyz/chrome-kasa
    network_mode: "host"
    environment:
      chromecast: 'chromecast-friendly-name' #replace with the IP of your Kasa plug
      kasa: xxx.xxx.xxx.xxx #replace with the IP of your Kasa plug
      idle: 30 #set how long plug should stay turned on for, remove line to have default of 60